@model IEnumerable<string>

@Html.ActionLink("Клиенты", "List", "Order")
@foreach (var link in Model)
{
    @Html.RouteLink(link, new
{
    controller = "Order",
    action = "List",
    category = link,
    page = 1
},
    new
    {
        @class = link == ViewBag.SelectedCategory ? "selected" : null
    })
}